# Chave de API do OpenRouter (obrigatória para usar a IA)
OPENROUTER_API_KEY=sua api key

# Modelo OpenRouter (opcional, valor padrão já configurado)
OPENROUTER_MODEL=openrouter/free

# Porta do servidor (opcional, padrão 3000)
PORT=3000